## CRUD operation - Features
- Account CRUD APIs
- JdbcTemplate-based DAO implementations
- Clean layered architecture (controller → service → dao)
- Lombok to reduce boilerplate
- Oracle-compatible `schema.sql`
